import "@std/io/print";

mod Abc<A, B> {
    let asd : num = 10 * (25 + 10 + (25 * 10 / (9 - 4)));

    fun Abc() {
        println("My super cool constructor");
    }
}

mod Bcd<C, D> {
    let asd : num = 10 * (25 + 10 + (25 * 10 / (9 - 4)));

    fun Bcd() {
        println("My super cool constructor");
    }
}

fun main() {
    // TODO: Fix this isn't throwing type mismatch error
    let my_instance : Bcd<num, bool> = new Abc<>();

    print("Hello, World!");
}